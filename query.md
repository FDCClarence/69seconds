do the ff

Make the UI more minimalistic. there is too much going on. 

Simple login/register on landing page. 

post landing page is simply a menu on the center with a "Create room" and "Join Room" with a room code input

Topbar will simply be a [User profile picture] [User email] [ down arrow to signify menu ]

the menu will simply have a logout for now.

also make the registration use "Username" "Email" "Password". the user can login via username or email